{
  "properties": [
    {
      "name": "cipherchat.jwt.expiry-seconds",
      "type": "java.lang.Long",
      "description": "Time in seconds after which the JWT token expires. Defaults to 3600 seconds (1 hour)."
    },
    {
      "name": "cipherchat.refresh.expiry-seconds",
      "type": "java.lang.Long",
      "description": "Time in seconds after which the refresh token expires. Defaults to 1209600 seconds (14 days)."
    }
  ]
}